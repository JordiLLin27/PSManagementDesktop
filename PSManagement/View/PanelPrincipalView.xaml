<Window x:Class="PSManagement.View.PanelPrincipalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PSManagement.View"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="PSManagement" Height="470.021" Width="839.089" WindowState="Maximized" Icon="/Assets/shipping_shipment_product.ico" WindowStyle="None" ResizeMode="NoResize">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Archivo">
                <MenuItem Header="_Nuevo"></MenuItem>
            </MenuItem>
            <MenuItem Header="Gestion"></MenuItem>
            <MenuItem Header="Opciones">
                <MenuItem Header="Modo oscuro" IsCheckable="True" IsChecked="False"></MenuItem>
            </MenuItem>
        </Menu>
        <DockPanel DockPanel.Dock="Left">

            <DockPanel x:Name="PanelBotonesAccionesStackPanel" DockPanel.Dock="Top">
                <Button x:Name="VentasButton"  DockPanel.Dock="Top" Margin="5" Foreground="White" Height="50" Width="200" FontSize="24" Click="VentasButton_Click">Ventas</Button>
                <Button x:Name="GestionButton"  DockPanel.Dock="Top" Margin="5" Foreground="White" Height="50" Width="200" FontSize="24">Gestión</Button>
                <Button x:Name="InventarioButton"  DockPanel.Dock="Top" Margin="5" Foreground="White" Height="50" Width="200" FontSize="24">Inventario</Button>
            </DockPanel>

            <DockPanel x:Name="PanelBotonesOpcionesDockPanel" DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                <Button x:Name="SalirButton" DockPanel.Dock="Bottom" Margin="5" Click="Button_Click" Content="Salir" Foreground="White" Height="50" Width="200" FontSize="24"/>
                <Button x:Name="OpcionesButton" DockPanel.Dock="Bottom" Margin="5" Content="Opciones" Foreground="White" Height="50" Width="200" FontSize="24"/>
            </DockPanel>

        </DockPanel>
        <Grid x:Name="PanelDeTrabajoGrid">
            <Label HorizontalAlignment="Center" VerticalAlignment="Center">Bienvenido</Label>
            <local:VentasView x:Name="PanelVentasVentasView" Visibility="Hidden"/>
        </Grid>
    </DockPanel>
</Window>
