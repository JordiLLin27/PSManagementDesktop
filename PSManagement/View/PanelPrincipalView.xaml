<Window x:Class="PSManagement.View.PanelPrincipalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PSManagement.View"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="PSManagement" Height="470.021" Width="839.089" WindowState="Maximized" Icon="/Assets/shipping_shipment_product.ico" WindowStyle="None" ResizeMode="NoResize">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Archivo">
                <MenuItem Header="_Nuevo"></MenuItem>
            </MenuItem>
            <MenuItem Header="Gestion"></MenuItem>
            <MenuItem Header="Opciones">
                <MenuItem Header="Modo oscuro" IsCheckable="True" IsChecked="False"></MenuItem>
            </MenuItem>
        </Menu>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal">
            <Button Margin="5" Foreground="White" Height="50" Width="200" FontSize="24">Gestión</Button>
            <Button Margin="5" Foreground="White" Height="50" Width="200" FontSize="24">Ventas</Button>
            <Button Margin="5" Foreground="White" Height="50" Width="200" FontSize="24">Inventario</Button>
            <Button Margin="5" Click="Button_Click" Content="Salir" Foreground="White" Height="50" Width="200" FontSize="24"/>
        </StackPanel>
        <Grid x:Name="PanelDeTrabajoGrid">
            <Label HorizontalAlignment="Center" VerticalAlignment="Center" Margin="326,137.822,326,164" Grid.Row="1">Bienvenido</Label>
        </Grid>
    </DockPanel>
</Window>
